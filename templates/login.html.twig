{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Login{% endblock %}

{% block stylesheet %} {{ parent() }}

   <style type="text/css">
        .login-head {
            padding-top: 150px;
            height: 700px;
            width: auto;
            background: linear-gradient(to top, rgba(22, 22, 22, 0.1) 0%, rgba(22, 22, 22, 0.5) 75%, #161616 100%), url("/images/rider_login.jpg");
        }

    </style>

{% endblock %}


{% block content %}
    {#<div class="row">
         <h1>Login</h1>
     </div>
     <div class="row">

         {{ form_start(form) }}
         {{ form_row(form.username) }}
         {{ form_row(form.password) }}

         <button type="submit">Envoyer</button>
         {{ form_end(form) }}
     </div>
     <br/>
     <div class="row">
         <a href="{{ path('forgot_password') }}">Mot de passe oublié !</a>
     </div>
     <br/>
     <div class="row">
         <a href="{{ path('user_registration') }}">Créer un compte</a>
     </div>#}
    {#<div class="row">
        <section class="container">
        <div class="row" >
            <div class="modal-dialog">
                <div class="loginmodal-container">
                    <h1>Login to Your Account</h1><br>
                    {{ form_start(form) }}
                    {{ form_row(form.username) }}
                    {{ form_row(form.password) }}

                    {#<input type="submit" name="login" class="login loginmodal-submit">#}
                    {#<button type="submit" class="login loginmodal-submit">Login</button>
                    {{ form_end(form) }}
                    <div class="login-help">
                        <a href="{{ path('user_registration') }}">Register</a> - <a href="{{ path('forgot_password') }}">Forgot Password</a>
                    </div>
                </div>
            </div>
        </div>
        </section>
    </div>#}


    <div>
        <a id="cAller" class="cVisible" href="#login_form">
        </a>
    </div>
    <header class="login-head">
        <div class="container d-flex h-100 align-items-center">
            <div class="mx-auto text-center">
                <div class="">
                    <div class="row">
                        <div class="login-form" id="login_form">
                            <h1>Login to Your Account</h1><br>
                            {{ form_start(form) }}
                            {{ form_row(form.username) }}
                            {{ form_row(form.password) }}

                            {#<input type="submit" name="login" class="login loginmodal-submit">#}
                            <button type="submit" class="login loginmodal-submit">Login</button>
                            {{ form_end(form) }}
                            <div class="login-help">
                                <a href="{{ path('user_registration') }}">Register</a> - <a href="{{ path('forgot_password') }}">Forgot Password</a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

    </header>



    <div class="container login-header"  >
        <section class="login-img ">

            <p>Image de snowboarder qui se prépare à rider !</p>
        </section>



    </div>


{% endblock %}

{% block javascript %} {{ parent() }}
{% endblock %}